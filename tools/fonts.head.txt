/* ============================================================
   덱에 심은 글꼴 — build.sh 가 00_head.html 의 @FONTS@ 자리에 끼워 넣는다.
   손으로 고치지 말 것. tools/fonts.py 가 다시 만든다.

   Pretendard    © 길형진 (orioncactus) · SIL Open Font License 1.1
   IBM Plex Mono © IBM Corp.            · SIL Open Font License 1.1

   덱에 실제로 쓰인 글자(한글 549자 포함 709자)만 남긴 서브셋이다. 그래서 인터넷이
   없어도 글꼴이 그대로 나온다. 새 글자를 넣었는데 모양이 달라 보이면 서브셋에 없는
   것이니 tools/fonts.py 를 다시 돌린다 — tools/check.js 가 빠진 글자를 일러 준다.
   ============================================================ */
