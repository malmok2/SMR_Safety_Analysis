# 변경 이력

## v2.0 — 2026-09-15
- **한 학기치를 주차별로 쌓는 구조로 갈아엎었다.** 덱 하나를 전제로 짠 `src/` 를 공용(`src/common/`)과
  주차별(`src/decks/<슬러그>/`)로 갈랐다. 슬러그는 주차 + 영문 하이픈 표기 —
  `w02-03_smr-design-requirements`. 폴더 이름이 그대로 파일 이름과 URL 이 된다.
- **`docs/index.html` 은 이제 강의 목차 페이지다.** `deck.conf` 들을 읽어 주차 카드를 만들고,
  카드마다 슬라이드(한/영)·PDF 링크를 단다. 색·글꼴은 덱에서 그대로 가져온다.
- **빌드를 `tools/build.py` 로 옮겼다.** 덱이 여럿이면 `cat` 한 줄로는 안 되고, bash 와 PowerShell 에
  같은 로직을 두 벌 두면 갈라진다. `build.sh` · `build.ps1` 은 이제 껍데기다.
- `tools/check.js` · `tools/pdf.js` 도 덱을 돌면서 일한다. 덱 이름을 주면 그것만 한다.
- 덱 소스는 파일마다 `<script>…</script>` 로 닫힌다. 공용 `99_boot.html` 이 제 script 에서 `boot()` 를
  부른다 — 새 주차를 만들 때 닫는 것을 잊어도 깨지지 않게.

## v1.6 — 2026-09-15
- **모노 문맥의 한글이 Pretendard가 아니었다.** `--mono` 스택(`IBM Plex Mono` → `SFMono` →
  `D2Coding` → `Consolas`)에 한글을 가진 글꼴이 하나도 없어, 축 라벨·판독기 키·태그·표지 끝줄의
  한글이 OS 기본 글꼴(맑은 고딕)로 떨어지고 있었다. 스택 뒤에 Pretendard를 넣어 고쳤다.
- **글꼴을 파일 안에 심었다.** 덱에 쓰인 글자(한글 553자 포함 713자)만 남긴 Pretendard 5벌 ·
  IBM Plex Mono 4벌 서브셋 388 KB를 base64로 넣었다. 인터넷이 없어도 글꼴까지 그대로 나온다.
  CDN 링크는 서브셋에 없는 글자를 위한 보조로만 남겼다. 덱 200 KB → 731 KB,
  한국어 PDF 는 오히려 3.4 MB → 1.4 MB(전체 글꼴 대신 서브셋을 품어서).
- `tools/fonts.py` 추가 — 서브셋을 다시 만든다. `tools/check.js`가 서브셋에 없는 글자를 잡는다.
- **PDF 파일 이름을 강의자료 제목으로.** `SMR_설계요건과_공학적안전설비.pdf` ·
  `SMR_Design_Requirements_and_Engineered_Safety_Features.pdf`.

## v1.5 — 2026-09-15
- **조작 막대를 무대 아래 46 px 띠로 꺼냈다.** 한/영 버튼이 11 px 반투명 칩으로 있다가 4초 뒤 사라져
  사실상 없는 것과 같았다. 이제 `한국어 / English` · 배경 전환 · `PDF ↓` 가 늘 떠 있고, `fit()`이
  띠만큼 자리를 비워 두므로 어떤 화면비에서도 슬라이드를 덮지 않는다.
- `F` 전체화면에서는 띠가 사라지고 슬라이드가 화면을 다 쓴다(`fullscreenchange`에 `fit()` 연결).

## v1.4 — 2026-09-15
- **영문 HTML 파일을 따로 굽는다.** `docs/index_en.html` — 내용은 같고 영어로 열린다.
  `L` 키만으로는 건네받은 사람이 한국어 화면을 먼저 보게 되므로, PDF처럼 파일을 둘로 나눴다.
  기본 언어 우선순위는 `?lang=` > 파일의 기본값 > localStorage > ko.
- 표지 조작 안내에 `T`(배경)를 넣었다.
- `tools/check.js`가 영문판 파일이 실제로 영어로 열리는지도 확인한다.

## v1.3 — 2026-09-15
- **판면 정리.** 그림이 본문 영역의 52 %밖에 못 채우던 슬라이드가 있었다(위아래로 108 px씩 빈 띠).
  16장의 viewBox 높이를 키우고 내용을 다시 앉혀 **전부 83 % 이상**으로 올렸다.
- **측면 패널이 있는 슬라이드의 그림이 0.73배로 축소 렌더링되고 있었다.** viewBox 폭 1120을
  816 px 칸에 넣은 탓으로, 12 px 라벨이 화면에서 8.8 px 로 나왔다. 11·13·27장을 820 폭으로 다시
  배치해 1:1 로 맞췄다(11장은 두 패널을 위아래로 쌓았다).
- 2장 세 번째 기능 상자가 viewBox(330) 밖 340까지 내려가 **아랫변이 잘려 있던 것**을 고쳤다.
- 37장 유로 `<path>`를 `Z`로 닫아 생긴 대각선이 노심 위를 가로질러 삼각형을 만들던 것을 고쳤다.
- 영문 제목 8개가 두 줄로 흘러 본문이 43 px 밀리던 것을 한 줄로 줄였다.
- 조작부(KO/EN/LIGHT/PDF)를 무대 바깥 아래 띠로 내렸다. `THINK LAB · HANYANG` 위에 얹히던 문제.
- `tools/check.js`에 **판면 감사**를 넣었다 — 제목 줄 수, 그림이 본문을 채우는 비율.

## v1.2 — 2026-09-15
- **본문 글씨체를 Pretendard로.** 라벨·수치는 IBM Plex Mono 유지.
- **밝은 배경 추가.** `T` 키 · 오른쪽 아래 버튼으로 전환. 색이 지는 의미는 그대로 두고 명도만 뒤집었고,
  강조 4색은 흰 바탕에서 WCAG AA를 넘도록 다시 잡았다. **인쇄·PDF는 밝은 배경이 기본**이다.
  SVG에 직접 박혀 있던 색 14곳을 토큰(`--on-accent` `--hot-2` `--dead` `--bubble` `--act` `--veil`)으로 뺐다.
- **SVG 글자 겹침 11곳 수정.** `tools/check.js`가 HTML 요소만 보고 SVG `<text>`는 전혀 보지 않고 있었다.
  인쇄 모드에서 46장을 한 번에 훑는 충돌 검사를 넣고, 거기서 나온 것을 모두 고쳤다.
  - 4장 산점도는 150~340 MWe 구간에 라벨 5개가 몰려 서로를 덮었다(한국어판도 마찬가지). 노형마다 한 행을
    주는 가로 막대로 바꿨다.
  - 4장 300 MWe 경계선이 v1.0부터 아예 그려지지 않고 있었다. `<line>`의 `y2`는 Chrome에서 CSS 속성이
    아니라 WAAPI가 무시한다.
  - 19장 사고 토큰이 각 단계의 설명 글자 위에 얹혀 있었다. 제목과 설명 사이 빈 통로로 옮겼다.
  - 2·7·11·12·15·21·24·27·32·40장 라벨 위치·viewBox·문구 길이 조정.

## v1.1 — 2026-09-15
- **한국어 · 영어 전환.** 46장 전체를 영문화했다. 소스에 `[[한국어||English]]` 토큰으로 나란히 담고,
  `L` 키 · 화면 오른쪽 아래 `KO/EN` 버튼 · 주소 `?lang=en` 로 전환한다. 선택은 브라우저에 기억된다.
  init 안에서 JS로 만드는 글자는 `TX(ko,en)`을 쓴다.
- **인쇄 · PDF 모드.** `P` 키(또는 `?print=1`)로 46장을 한 지면에 한 장씩 펼친다. 애니메이션은
  3.4초 뒤 멎은 장면으로 굳는다. `Ctrl+P`로 직접 저장하거나, 미리 구워 둔 PDF를 내려받는다.
- `docs/NUE4067_SMR_safety_ko.pdf` · `_en.pdf` 추가 (각 46쪽, 1280×720 가로).
- `tools/pdf.js`(PDF 굽기) · `tools/check.js`(전수 점검) · `tools/browser.js`(크로미움 탐색) 추가.
  `check.js`는 한/영 양쪽으로 46장을 넘기며 콘솔 오류 · 번역 누락 · 토큰 찌꺼기 · 레이아웃 넘침을 센다.
- 36번 슬라이드(압력 계층)에서 `<circle cy="NaN">` 오류를 고쳤다. `raf()` 첫 프레임의 경과시간이
  음수가 되어 `log10(t<1)`이 NaN을 만들던 문제로, 엔진에서 0으로 막았다. 다른 슬라이드에도 같은 위험이 있었다.
- 슬라이드 수는 46장 그대로.

## v1.0 — 2026-09-13
- 최초 제작. 46장, 11개 섹션.
- 교재 『SMR 설계요건과 공학적안전설비 (Rev1.0)』 10장 체계를 그대로 따름.
- 애니메이션·인터랙션 15종(규모의 경제 · 배관 파단 · 붕괴열 · 자연순환 · 열관성 · 인허가 타임라인 ·
  심층방어 · Category A~D · 제어봉 낙하 · SDM 계산기 · 잔열제거 수조 · RHR 3방식 ·
  압력 계층 인계 · NuScale ECCS 폐루프 · 격납 압력).
- 인허가 현황은 2026-09 기준.

## 2026-10 — 3~4주차 덱 추가 (73장)

교재 『THINK Lab SMR 자료를 찾는 법과 PWR형 SMR (Rev1.0)』을 73장으로 옮겼다.
1부(1~4장)는 자료를 확정하는 절차, 2부(5~10장)는 그 절차로 PWR형 SMR 세 기를 읽는 연습이다.

엔진에 셋을 더했다.
- `s.abbr` — 푸터 맨 윗줄의 약어 풀이. 어려운 약어가 나오는 장에서 그 자리에 뜻을 단다.
- `wrapT(s, n, lat)` · `widT(s, px, lat)` — SVG 안에서 줄을 나눈다. 한글은 공백이 드물어
  단어 기준으로 자르면 칸을 넘으므로 글자 폭으로 센다.
- `tools/figfit.js` — 그림 칸에 viewBox 높이가 맞는지 재서 권장값을 알려 준다.

고친 것
- WAAPI 로 `rect` 의 `width` 를 애니메이션할 때 맨 숫자를 주면 Chrome 이 조용히 무시한다.
  `'px'` 를 붙여야 한다. w02-03 4장의 SMR 구역 띠가 v1.0 내내 안 칠해지고 있었다.
- HTML 만 떼어 배포하면 인쇄 모드의 '미리 만든 PDF 내려받기' 가 죽은 링크가 되던 것.

## 2026-10 — 3~4주차 덱에 교재 그림을 넣었다 (73 → 86장)

처음 만들 때 교재의 사진·화면 캡처를 하나도 넣지 않고 전부 SVG 로 다시 그렸다.
ARIS 검색 화면이나 NuScale 모듈 실물처럼 **그림 자체가 내용인 것**까지 추상 도형으로
바꿔 버렸고, ARIS 슬라이드의 설계 상태 막대그래프는 아예 **비율을 지어냈다**(6절 4번 위반).
교재 그림 37장을 넣고 그 슬라이드들을 다시 만들었다.

- 새 경로 — `tools/media.py` 가 docx 에서 그림을 뽑아 흰 여백을 자르고 WebP 로 줄인다.
  `build.py` 가 `media/*.webp` 를 base64 로 심어 `window.IMG` 에 꽂고, 슬라이드는 `PH()` 로 쓴다.
  단일 파일 규칙은 그대로다.
- 원본으로 교체 — ARIS 화면(3장) · NRC 페이지 · PWR/BWR 계통도 · NuScale 모듈 단면 ·
  i-SMR 모듈 단면 · BWRX-300 원자로용기와 유동 경로.
- 빠져 있던 교재 절을 채웠다 — 6.7 원자로건물과 부지 · 6.8 나선형 증기발생기 ·
  6.9 공장 제작과 EPZ · 9.1 건물과 계통 · 9.3 격리밸브와 건식 격납 · 9.6 FMCRD ·
  2.2.1 부유식(Akademik Lomonosov) · 2.4.3 설계 카드 · 2.4.4 인쇄판 집계 · 2.4.6 경제성.
- `check.js` 에 열 번째 항목 — **사진이 너무 작게 그려진 장**. 한 줄에 셋을 넘게 넣거나
  캡션이 길면 그림이 눌린다. 세로 200 px 이 하한이다.
- `tools/pdfshrink.py` — 크로미움이 인쇄 때 그림을 2배로 키워 넣는 바람에 PDF 가 25 MB 가
  되었다. 사진만 JPEG 로 다시 눌러 7 MB 로 줄인다. `pdf.js` 가 끝에 자동으로 부른다.
